/**
 * Ionic core and utility stylesheets, loaded exactly once through the
 * Ionic owner. Importing the Ionic facade loads them as a side effect.
 * Bundled through `ionic-base.css`, which wraps every one of these in an
 * explicit `layer(ionic)` so the app's own (unlayered) CSS always wins —
 * see that file's header comment.
 */
import './ionic-base.css';
