import { APP_ICONS } from '@/packages/icons';
import { TEST_IDS } from '@/shared/config';

export const AVATAR_FALLBACK_DEFAULT_TEST_ID = TEST_IDS.avatarFallback;
export const AVATAR_FALLBACK_ICON = APP_ICONS.person;
