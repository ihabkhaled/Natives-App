import { APP_ICONS } from '@/packages/icons';
import { TEST_IDS } from '@/shared/config';

export const ERROR_STATE_ICON = APP_ICONS.warning;
export const ERROR_STATE_DEFAULT_TEST_ID = TEST_IDS.errorState;
