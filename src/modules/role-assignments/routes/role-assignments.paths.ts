import { APP_PATHS } from '@/shared/config';

/** Route pattern and navigation target for the role-assignments admin screen. */
export function roleAssignmentsPagePath(): string {
  return APP_PATHS.roleAssignments;
}
