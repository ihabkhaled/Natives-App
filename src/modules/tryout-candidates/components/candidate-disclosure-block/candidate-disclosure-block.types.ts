import type { CandidateDisclosureView } from '../../types/tryout-candidates-view.types';

export interface CandidateDisclosureBlockProps {
  readonly view: CandidateDisclosureView;
  readonly testId: string;
}
