import { APP_PATHS } from '@/shared/config';

/** Route pattern and navigation target for the candidate review screen. */
export function tryoutCandidatesPagePath(): string {
  return APP_PATHS.tryoutCandidates;
}
