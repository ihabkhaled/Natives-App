import { TEST_IDS } from '@/shared/config';

export const WORKBENCH_LIST_TEST_IDS = {
  list: TEST_IDS.workbenchVirtualList,
  item: TEST_IDS.virtualListItem,
} as const;

export const WORKBENCH_LIST_HEIGHT_PX = 320;
